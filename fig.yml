db:
  image: postgres

app:
  build: .
  ports:
    - "3000"
  links:
    - db:pw-db

web:
  image: nginx
  ports:
    - "80:80"
  links:
    - app:pw-app
  volumes:
    - ./lib/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
